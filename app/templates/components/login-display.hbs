{{#if currentUser.curUser}}
  {{#link-to 'user' currentUser.curUser.id}}
    Hello, {{currentUser.curUser.username}}
  {{/link-to}}
{{else}}
  {{#link-to 'login'}}
    Login/Register
  {{/link-to}}
{{/if}}
