
import { adminCheck } from 'ember-qna/helpers/admin-check';
import { module, test } from 'qunit';

module('Unit | Helper | admin check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = adminCheck([42]);
  assert.ok(result);
});

