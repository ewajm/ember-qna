
import { userIcon } from 'ember-qna/helpers/user-icon';
import { module, test } from 'qunit';

module('Unit | Helper | user icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userIcon([42]);
  assert.ok(result);
});

